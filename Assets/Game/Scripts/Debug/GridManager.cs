using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[ExecuteAlways]
public class GridManager : MonoBehaviour
{
    [SerializeField] CanvasGroup[] grids;
    [SerializeField] Canvas grid;


    void Update()
    {/* 
        bool isGridEnabled = Input.GetKey(KeyCode.LeftAlt) && Input.GetKey(KeyCode.LeftShift) && Input.GetKeyDown(KeyCode.G); 

        if (isGridEnabled)
            grid.enabled = grid.enabled == true ? false : true;
        
        if (Input.GetKey(KeyCode.LeftAlt) && Input.GetKey(KeyCode.G) && Input.GetKeyDown(KeyCode.Alpha1))
            grids[0].alpha = grids[0].alpha == 1 ? 0 : 1; 

        if (Input.GetKey(KeyCode.LeftAlt) && Input.GetKey(KeyCode.G) && Input.GetKeyDown(KeyCode.Alpha2))
            grids[1].alpha = grids[1].alpha == 1 ? 0 : 1; 
        
     */}
}
